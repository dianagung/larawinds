<div class="my-3">
    <label class="uppercase px-2 text-xs text-slate-500 font-semibold block mb-2" for="{{ $name }}">{{ $label }}</label>
    <textarea id="{{ $name }}" class="px-3 py-3 border border-slate-300 rounded-md w-full" placeholder="{{ $placeholder }}">{{ $value }}</textarea>
</div>